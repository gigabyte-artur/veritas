
&НаКлиенте
Процедура ПриОткрытии(Отказ)

КонецПроцедуры

&НаКлиенте
Процедура КомандаНаградаЗолото(Команда)
	СтруктураЗакрытия = Новый Структура();
	СтруктураЗакрытия.Вставить("Награда", ПредопределенноеЗначение("Перечисление.ВидыПараметровИгроков.Золото"));
	СтруктураЗакрытия.Вставить("Размер", 5);
	Закрыть(СтруктураЗакрытия);
КонецПроцедуры

&НаКлиенте
Процедура КомандаНаградаКарта(Команда)
	СтруктураЗакрытия = Новый Структура();
	СтруктураЗакрытия.Вставить("Награда", ПредопределенноеЗначение("Справочник.Карты.ПустаяСсылка"));
	СтруктураЗакрытия.Вставить("Размер", 2);
	Закрыть(СтруктураЗакрытия);
КонецПроцедуры

&НаКлиенте
Процедура КомандаНаградаУрон(Команда)
	СтруктураЗакрытия = Новый Структура();
	СтруктураЗакрытия.Вставить("Награда", ПредопределенноеЗначение("Перечисление.ВариантыДействийПротивников.Атака"));
	СтруктураЗакрытия.Вставить("Размер", 8);
	Закрыть(СтруктураЗакрытия);
КонецПроцедуры

&НаКлиенте
Процедура КомандаНаградаЭнергия(Команда)
	СтруктураЗакрытия = Новый Структура();
	СтруктураЗакрытия.Вставить("Награда", ПредопределенноеЗначение("Перечисление.ВидыПараметровИгроков.Энергия"));
	СтруктураЗакрытия.Вставить("Размер", 1);
	Закрыть(СтруктураЗакрытия);
КонецПроцедуры

&НаКлиенте
Процедура КомандаНаградаЩит(Команда)
	СтруктураЗакрытия = Новый Структура();
	СтруктураЗакрытия.Вставить("Награда", ПредопределенноеЗначение("Перечисление.ВидыПараметровИгроков.Щит"));
	СтруктураЗакрытия.Вставить("Размер", 6);
	Закрыть(СтруктураЗакрытия);
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ЭнергияИгрока = Параметры.ЭнергияИгрока;
	ЗдоровьеПротивника = Параметры.ЗдоровьеПротивника;
	МаксимальноеЗдоровьеПротивника = Параметры.МаксимальноеЗдоровьеПротивника;
	МодификаторыИгрока = Новый СписокЗначений;
	Для Каждого ЭлементСписка Из Параметры.Модификаторы Цикл
		МодификаторыИгрока.Добавить(ЭлементСписка.Значение);
	КонецЦикла;
	УправлениеДоступностью();
	Элементы.ДекорацияЭнергияИгрока.Заголовок = "Энергия: " + Строка(ЭнергияИгрока);
	Элементы.ДекорацияЗдоровьеПротивника.Заголовок = "Противник: " + Строка(ЗдоровьеПротивника) + "/" + Строка(МаксимальноеЗдоровьеПротивника);
КонецПроцедуры

// Управляет видимостью кнопок наград в зависимости от наличия модификаторов у игрока.
//
&НаСервере
Процедура УправлениеДоступностью()
	Если ЕстьМодификаторИгрока(ПредопределенноеЗначение("Справочник.Модификаторы.ПлащМеркурия")) Тогда
		Элементы.КомандаНаградаЗолото.Видимость = Истина;
	КонецЕсли;
	Если ЕстьМодификаторИгрока(ПредопределенноеЗначение("Справочник.Модификаторы.ШлемАфины")) Тогда
		Элементы.КомандаНаградаЩит.Видимость = Истина;
	КонецЕсли;
КонецПроцедуры

// Определяет, есть ли у игрока заданный модификатор.
//
// Параметры:
//  МодификаторВход - СправочникСсылка.Модификаторы - проверяемый модификатор.
//
// Возвращаемое значение:
//  Булево - Истина, если модификатор есть у игрока.
//
&НаСервере
Функция ЕстьМодификаторИгрока(МодификаторВход)
	Возврат МодификаторыИгрока.НайтиПоЗначению(МодификаторВход) <> Неопределено;
КонецФункции
